# Decision ledger (project decisions — not the in-season decision ledger)

Settled questions, so nobody (human or model) re-litigates them. One line
each; link an ADR when the reasoning is long. The in-season START/SIT/WAIVER
ledger is data (`data/ledger/`), not this file.

| Date | Decision | Why / where |
|---|---|---|
| 2026-09-03 | Package name `gridiron`, repo lives in `Projects/Football` | valid importable identifier; bootstrap doc suggested it |
| 2026-09-03 | Ported run_summary.py verbatim; smoke/golden_run adapted (lists only) | ADR-0001 |
| 2026-09-03 | League settings are placeholders gated by `SETTINGS_VERIFIED=False` | real settings unavailable at bootstrap; rule #1 |
| 2026-09-03 | Stat keys follow nflverse weekly column names | no renaming at join boundary; scoring.py |
| 2026-09-03 | golden_run TARGETS starts empty; register pipelines as they land | no pipelines exist yet |
| 2026-09-04 | Ingest uses `nflreadpy`, NOT `nfl_data_py` (deprecated, repo archived 2025-09-25, last release 2024-09) | bootstrap doc §3 is stale on this; research draft data2026 |
| 2026-09-04 | Betting lines come free from nflverse `load_schedules()` (`spread_line`, `total_line`, moneylines, roof/wind); The Odds API only if we need intraweek line moves | ~108 credits/season needed vs 500/mo free tier |
| 2026-09-04 | nflverse `spread_line` sign is HOME-POSITIVE; convert at ingest via `vegas.implied_totals_from_nflverse` | verified corr(spread_line, home margin)=+0.49 on 2024 |
| 2026-09-04 | Sportsbook sign convention (negative = favored) is the package-internal one | `vegas.implied_team_total` docstring |
| 2026-09-04 | Build-step-3 baseline shape: volume from usage priors, efficiency = positional prior × line multiplier (`vegas.receiving_efficiency_multiplier`). NOT "share × implied total" | Vegas signal is 92% efficiency for receivers; volume-on-line has ~0 OOS skill (QUANT_FOUNDATIONS §1.4) |
| 2026-09-04 | Line→scoring/efficiency coefficients live in `gridiron.vegas`; volume functions are documented as league means only | all passed leave-one-season-out (rule #5); volume did not |
| 2026-09-04 | Positional efficiency stored as components (catch, Y/T, TD/T); points computed via `gridiron.scoring` weights | rule #2 — constants follow real scoring once rule #1 flips |
| 2026-09-04 | Variance-seeking threshold: chase ceiling only when trailing by ≳6 projected pts; Φ derivative alone is wrong-signed at deficit 5 | gamma-marginal MC, QUANT_FOUNDATIONS §2.4 |
| 2026-09-04 | Ingest client uses a plain script User-Agent (never spoof a browser); nflverse lines = DraftKings close via ESPN | site.api.espn.com 403s browser UAs only |
| 2026-09-04 | Pin `pandas>=2.0,<4` at ingest; test on 3.x | `>=2.0` resolves to 3.0.5 (copy-on-write, new string dtype) |
| 2026-09-04 | §5-7 of QUANT_FOUNDATIONS ship as UNVERIFIED; their constants live in `shrinkage.py` behind an explicit warning banner and may not reach user-facing output | verifier agents were killed by a usage limit; sibling verified sections had 14 corrections |
| 2026-09-04 | Shrinkage n0 for WR/TE target share = 90 team targets (~3 games); RB carry-share prior decays to zero by week 6 | out-of-sample RMSE grid vs rest-of-season share (QUANT_FOUNDATIONS §5.3) |
| 2026-09-04 | Waiver/start-sit replacement is always a FORWARD max over the pool, never a season-to-date rank | Henderson 2025: RB42 through wk 8, then 17.0 PPG (§6.6) |
| 2026-09-04 | Treat the flex as a WR slot when shaping replacement level (~10 of 12 flex slots are WR in full PPR) | order-statistic fill on 2023-25 (§6.2); revisit if the league is half-PPR |
| 2026-09-04 | FAAB: never bid 10-19% of budget; bid $1 above focal round numbers; expect optimal bids to RISE through the season | dead-zone clearing data + shadow-price dynamic program (§7) |
| 2026-09-04 | The §5-7 verifier SCRIPTS are committed and re-runnable even though their agents were killed; their pass/fail counts are the doc's evidence of record | 381/382, 177/12, 66/9 — better than no verification, worse than a reconciled one |
| 2026-09-08 | League settings VERIFIED from the Sleeper API: 12-team half-PPR, INT −1, QB/2RB/2WR/TE/2FLEX/K/DEF + 5 BN + 1 IR, 15-round snake, slot 1 | `pull_sleeper.py`; `SETTINGS_VERIFIED=True` flipped in the same commit as the values |
| 2026-09-08 | `DEFAULT_SCORING` IS the league's rules; tests needing full-PPR pass `ScoringRules(reception=1.0)` explicitly | rule #2 — one implementation, real weights |
| 2026-09-08 | Draft projections = ½ Sleeper stat-line (scored in-repo) + ½ ECR-implied points; replacement by lineup order-statistic fill (RB33/WR41/TE13/QB13) | `draft_board_2026.py`; FantasyPros projection pages are JS-paginated |
| 2026-09-08 | Draft availability uses a 60/40 Sleeper/FFC ADP blend with sd = 0.57 + 0.11·ADP | the room drafts on Sleeper; sd fitted on FFC per-player spread |
| 2026-09-08 | Draft policy: best static VOR each pick, K/DEF last two rounds; elite TE at the 2/3 turn | beat scripted RB/WR openings by ~100 lineup pts over 300 sims (`DRAFT_2026_PLAN.md`) |
| 2026-09-08 | War-room page logic lives in ONE place per language: `gridiron/draft.py` (Python) and `scripts/research/warroom/draftroom_logic.js` (page), each with tests pinning the same numbers | audit found the page and the script had drifted into two hand-copies of the same math |
| 2026-09-08 | Page confirmations never use `window.confirm`/`prompt` (the artifact sandbox swallows them); use in-page two-tap controls | Reset button silently did nothing |
| 2026-09-17 | League settings RE-VERIFIED live (55/55 constants, zero drift); CLAUDE.md rule #1's "unverified placeholders" prose was stale, the flag was not | `scripts/verify_league_settings.py`; the Sept-8 pull was real, the headline just never followed it |
| 2026-09-17 | Verification is standing, not one-off: a checker script + pinned values in `tests/test_league_config.py`; the checker is AST-tested to be write-free | leagues get edited mid-season; a checker that can edit what it checks can flip a flag to pass |
| 2026-09-17 | A constant absent from the live payload is reported as drift (`live=None`), never skipped; omitted kicking/defense weights are counted in the output | a check that silently covers less than it did last week fails the same way as one that passes wrongly |
| 2026-09-17 | Scoring reads the CURRENT nflverse columns (`passing_interceptions`, the three `*_fumbles_lost`, the three `*_2pt_conversions`) with the legacy names kept as read-only fallbacks | the rewritten nflverse schema made INTs, fumbles and 2-pointers score as ZERO — a wrong number, not a missing one |
| 2026-09-17 | `fumbles_lost_total` is NOT the fumble input: it counts return fumbles | 2 mismatches vs nflverse on the 2026 wk-1 frame; the 3-component sum reconciles exactly (357/357) |
| 2026-09-17 | Kicker points ship (`scoring.kicker_points`, KICKING_SCORING onto nflverse `fg_made_*`/`pat_*`); blocked kicks deliberately unmapped | 12/12 exact vs Sleeper actuals; no blocked kick in wk 1, so the blocked-as-miss question is UNVERIFIED |
| 2026-09-17 | Sleeper->gsis crosswalk source of record is dynastyprocess `db_playerids.csv`, fetched from raw.githubusercontent.com | 168/168 rostered players resolve vs 36/168 from Sleeper's own sparse `gsis_id`; `nflreadpy.load_ff_playerids()` 403s through the egress proxy |
| 2026-09-17 | Sleeper's own `gsis_id` is kept as an OVERLAY that only fills gaps, never overrides | a brand-new player dynastyprocess hasn't picked up still resolves without a name match |
| 2026-09-17 | The Sleeper adapter is read-only by construction and tested for it; lineups/waivers/trades/messages stay a human's job | `gridiron/sleeper.py`, `tests/test_sleeper.py::test_module_is_read_only` |
| 2026-09-17 | Freshness is cadence-aware in ET, not one TTL: injuries tighten from 48h to 12h on gameday/designation/waiver days | rule #8 — the week has a shape |
| 2026-09-17 | Report freshness is read from the ingest MANIFEST's pull time, never a file mtime | a file rewritten from cache is not fresher data |
| 2026-09-17 | "Not on this week's injury report" (evidence) and "no injury report loaded" (no evidence) render differently; a prior week's designation is labelled with its week | collapsing them is how a report tells you a hurt player is fine |
| 2026-09-17 | A team absent from a schedule we could not load renders `?`, not BYE | a missing schedule must not invent a bye week |
| 2026-09-17 | Milestone 1 ships NO projections, rankings or lineup advice; the report prints measured usage + market lines and says so at the top | rule #5 gate is unmet; a guessed recommendation presented as verified is the failure mode the repo exists against |
| 2026-09-17 | Weekly outputs are written to `data/outputs/week{NN}_report.*` plus a stable `weekly_report_latest.*` pair for golden_run A/B | golden_run compares literal paths; the week-stamped name changes weekly |
| 2026-09-17 | Scoring's test suite reads its fixtures with stdlib `csv`, not pandas, and uses no shared conftest fixture | the scoring implementation is stdlib-pure; its tests must run on the repo's declared dependencies alone or the purity claim is untested |
| 2026-09-17 | A report about week N reads weeks <= N-1 pregame, <= N once the slate is COMPLETE; the boundary comes from the report week and phase, never from `max(weeks in cache)` | re-rendering week 2 in week 6 silently read weeks 3-5 — a backtest that sees the future scores itself |
| 2026-09-17 | Weeks past the boundary are WITHHELD and named in the report, and a re-render says its injury designations are after-the-fact | withholding silently is its own dishonesty; the reader has to know the cache is ahead of the report |
| 2026-09-17 | A failed refresh records the failure ONLY: `path`/`rows`/`weeks`/`as_of` keep describing the last pull that succeeded | recording the failure as a new entry stamped it with `now`, so a dead network read as a current pull — and dropped the good cached file over a transient 503 |
| 2026-09-17 | A source whose latest refresh failed can never read FRESH; it renders STALE with the failure reason and still shows its cached rows | stale-but-real, labelled, beats an empty report; the newest thing that happened to that source is a failure and the reader is told |
| 2026-09-17 | Cache writes go through a temp file + `os.replace` | a pull that dies mid-write must not leave a truncated parquet where the last good one was |
| 2026-09-17 | Freshness carries the FULL covered-week set, not just the last; a hole inside the range is its own degradation line | "covers through wk5" off weeks {1,2,4,5} is true and misleading — every season total is short a week and nothing said so |
| 2026-09-17 | Season rollover is a refusal, not a note: a cache season, or a cached Sleeper state season, that disagrees with the requested season exits 2; Sleeper week 0 (offseason) exits 2 unless `--week` is passed | joining last season's roster to this season's box scores produces a confident wrong report |
| 2026-09-17 | The rendered weekly roster report is gitignored (rule #10 carve-out); league-wide id-keyed files stay committed | the `lineup` column is a statement about one manager's players, and the file regenerates from the cache in one command |
| 2026-09-17 | Owner-roster exposure already in history is left alone | rewriting shared history is a bigger hazard than the exposure; the rule binds what gets committed next |
| 2026-09-17 | nflreadpy/pandas/pyarrow approved for the project env only (owner, 2026-09-17); PR2's dependency-free scoring slice merged first and stays dependency-free | the scoring repair was worth shipping without waiting on the dependency call, and it still runs on the stdlib |
| 2026-09-17 | Every source the report READS declares its freshness; `sleeper_players` joined SOURCES | the report took the live injury designation, team, position and gsis overlay from a dump it never aged — a month-old "Questionable" printed under "All inputs current" |
| 2026-09-17 | `sleeper_players` gets an injury-sensitive cadence (24h, 6h on gameday/waiver/designation days) | it carries the live `injury_status`, the one field that can flip an hour before kickoff, and it is not week-keyed so age is the only signal there is |
| 2026-09-17 | A Sleeper designation from a non-FRESH player pull is rendered as STALE with the reason, and `AvailabilityNote.current` is False | the value is live, not week-keyed, so a month-old "Questionable" and a current one are the same five characters on the page |
| 2026-09-17 | `AvailabilityNote.designation_fresh` defaults to False, and `build_report` fails closed when `sleeper_players` is absent from the freshness list | a caller that never established currency is not entitled to the benefit of the doubt on the fastest-staling field |
| 2026-09-17 | `scoring_coverage()` checks a frame's COLUMNS against the league rules; alias-aware, weight-aware, and never reads a value | `fantasy_points` scoring an absent key as zero is right for a null cell and a wrong number for a missing column; the contract stays and this is the gate in front of it |
| 2026-09-17 | A partially present multi-column term (e.g. 2 of 3 lost-fumble columns) is a gap, not a pass | it sums what it has and returns a plausible number, which is worse than a blank one |
| 2026-09-17 | A missing scoring column BLANKS `pts`/`ppg` for the affected positions only, adds a named degradation, and fails `--fail-on-degraded`; usage columns are untouched | a target is a target whatever the scoring columns say, and the two halves of `gridiron.scoring` fail independently |
| 2026-09-17 | `build_report(scoring=...)` is a REQUIRED argument with no default | a default would let a caller publish points without ever looking at the schema behind them |
| 2026-09-17 | Scoring-input validation is BOTH persisted (`Entry.missing_columns`, written by the puller) and re-checked at read time by the report | a stderr warning dies with the run that printed it; the manifest is what the next reader has, and only a read-time check catches a cache edited or written by an older schema since |
| 2026-09-17 | A recorded schema defect is not an error: `error` stays empty, the file stays readable, the as-of stands | the fetch succeeded and the intact columns are still worth reading; conflating the two would discard good data over a renamed column |
| 2026-09-18 | Live league sync is a separate command from the weekly pull | The two have nothing in common but a hostname. League state changes inside an afternoon; nflverse frames and the 16 MB player dump do not, and Sleeper asks callers to fetch that dump once a day. One script on one cadence would have to pick the wrong cadence for one of them. | `scripts/sync/sleeper_sync.py`, five GETs, never touches nflverse or `players/nfl` |
| 2026-09-18 | Snapshots are immutable generations, not one overwritten filename | Overwriting `sleeper_league.json` and recording the manifest afterwards leaves a window in which the bytes are new and the as-of beside them is old. A reader landing in it ages fresh data by the previous pull's clock — or reads a week-3 roster under a week-2 as-of. A generation file cannot be the same object as the path already published. | `livesync.publish_snapshot` writes `sleeper_league_<stamp>.json`; the manifest entry is replaced atomically to point at it; 3 kept, referenced one never pruned |
| 2026-09-18 | The weekly puller publishes through the same helper | A lock only one of two writers takes is not a lock. `pull_week.py` wrote the same file and the same manifest entry with no lock at all. | `pull_week.pull_sleeper` calls `ls.publish_snapshot`; its final `manifest.save()` runs under the same lock and merges on-disk entries it did not touch |
| 2026-09-18 | The manifest is re-read inside the lock, and one entry is written | A writer that saves a manifest it loaded minutes ago erases everything another writer committed in between. This is how a five-minute sync would have silently deleted a concurrent nflverse pull's `weekly_stats` entry. | `publish_snapshot` reloads `Manifest` under the lock; `pull_week` merges untouched entries at save time |
| 2026-09-18 | Fetch, validate, then write — in that order | The only way a failure cannot corrupt the published snapshot is for the write to be unreachable until validation has passed whole. Timeout, rate limit, 404, empty list, short roster list, malformed body and wrong league all end in the same branch. | `livesync.sync_once`; 9 parametrised failure modes assert the previous bytes are byte-identical afterwards |
| 2026-09-18 | Identity is validated before publication, not after | Publishing a snapshot of the wrong league or the wrong season is the one failure that does lasting damage, and it is the one a retry never fixes. Owner presence is part of it: a league in which the configured owner holds no roster is not his league. | `validate_snapshot`: league id, league season, NFL-state season, team count, owner in users and owning a roster |
| 2026-09-18 | A rejection knows whether it is partial or wrong | Both refuse to publish, but "7 of 12 rosters" is a bad minute on the wire and "league 9999" is a configuration error a human must fix. Collapsing them would make the log useless. | `Rejection.partial`; exit code and `last_result` distinguish `partial` from `invalid` |
| 2026-09-18 | The read window is bounded, timed and stated | Sleeper offers five GETs, not one consistent read. Claiming a snapshot is atomic would be a lie the data cannot support. | `read_window_seconds` and a `consistency` string in every snapshot; NFL state read at both ends; a mid-read week change discards and retries once, twice means no publish |
| 2026-09-18 | Settings drift is detected every sync and verified by nobody | Rule #1 says verification is a human decision backed by a pull. A sync noticing a changed scoring weight is the trigger for that decision, not a substitute — but noticing it five minutes after it changes beats noticing it next week. | `settings_changes()` diffs watched keys against the previous snapshot; drift is recorded in sync state and printed on stderr; `SETTINGS_VERIFIED` is never written |
| 2026-09-18 | Sync health is a separate file from the ingest manifest | The manifest answers "how old is this data". It cannot answer "has the sync been failing for six hours with a good file still on disk", which is exactly what an operator needs to see. | `sync_state.json`: last success, last attempt, last error, failure streak, next due, drift |
| 2026-09-18 | The lock is OS-held (`fcntl.flock` / `msvcrt.locking`) on a persistent file that is never unlinked; no age, no takeover | A file-existence lock cannot be made race-free (two processes can both see it abandoned and both replace it); the kernel drops an OS lock when the holder dies. Superseded the `O_EXCL` + 240s design in `c1862c4`. | `livesync.single_writer`, two real-child-process tests |
| 2026-09-18 | Three independent guards against overlapping runs | The scheduler's `IgnoreNew` is the weakest of them and the only one a misconfigured task can lose. | Task `MultipleInstances IgnoreNew`, an execution time limit under the interval, the PowerShell hard timeout, and the Python file lock |
| 2026-09-18 | Scheduled task, not a Windows service | A service needs admin to install and a stored password or a system account to run. The worst case here is five minutes of staleness that `status` reports honestly, which does not justify either. The cost — it runs only while the desktop is on and signed in — is stated in the installer's own output. | `gridiron_task.ps1`, S4U logon, `RunLevel Limited`, no credential stored |
| 2026-09-19 | The dashboard ships the rule #5 BASELINE (usage prior × positional efficiency × line multiplier) and zero features on top of it, labelled UNVALIDATED on every render | ARCHITECTURE step 3: the baseline is what candidates are measured against; `validated_signals.FEATS` is empty and an unvalidated entry fails the import | `gridiron.projection`, `gridiron.models.validated_signals` |
| 2026-09-19 | Usage is shrunk toward the MEASURED role's prior (rank within team by targets/carries), efficiency is the positional prior × line multiplier; the player's own efficiency is never read | rule #4 (role from usage) and rule #6 (efficiency stabilizes 5–20× slower than usage); QUANT §1.4 (Vegas moves efficiency, not volume) | `projection.project`, `test_projection.py` |
| 2026-09-19 | QB passing and K priors are pooled from the admissible frame at run time, not hard-coded | QUANT carries no verified constant for them under this league's scoring; a measured number stated in `inputs` beats an invented one | `projection.build_evidence` → `PooledPriors` |
| 2026-09-19 | A team whose measured volume is under half the league mean gets the league mean, with a caveat on the row | a partial frame (a fixture, one box score in) would otherwise silently under-project everyone on it; the league mean is a verified constant (§5.1) | `TEAM_VOLUME_PLAUSIBILITY` |
| 2026-09-19 | Projections abstain (mean=None + reason) rather than fall back to a league average | a league-average number on a player with no evidence is indistinguishable on the page from a real projection | `projection.abstain`, `test_it_abstains_*` |
| 2026-09-19 | Questionable / Doubtful are flagged, never discounted; Out / IR / PUP / Sus are withheld to 0 with the model mean kept visible | rule #11; no verified availability model exists, so an adjustment would be a guess wearing a number | `dashboard.WITHHOLD_DESIGNATIONS` |
| 2026-09-19 | Unknown lock state (no schedule) abstains the whole start/sit and waiver sections | a lineup change Sleeper would reject is not an alternative; "unlocked" is not the safe default | `lineup.plan_lineup(locks_known=False)` |
| 2026-09-19 | An unprojected starter is frozen in place; an unprojected bench player is never proposed | "we do not know what he will score" is not evidence the other player is better | `lineup.plan_lineup` |
| 2026-09-19 | Every upgrade is an add/drop PAIR scored by the change in the best legal lineup; LINEUP and DEPTH gains are separate columns | rule #7 (the rejected side is half the decision); QUANT §6.1 (replacement is forward-looking and pool-shaped, never a season rank) | `waivers.build_board` |
| 2026-09-19 | P(win) is the closed form, labelled UNCALIBRATED everywhere, and abstains when any non-DST starter on either side is unprojected | no resolved-matchup calibration exists; QUANT §2.5 wants MC at small margins; a confident number would outrun the evidence | `dashboard._matchup`, `EvaluationReport.pwin_calibrated = False` |
| 2026-09-19 | The decision archive is written at render time and grading reads ONLY the archive plus actuals | rule #7; a grader that re-projects with later data has seen the future | `gridiron.decisions`, `test_grading_never_reads_anything_but_the_archive` |
| 2026-09-19 | Dashboard outputs and decision archives are gitignored (rule #10 carve-out, same as the weekly report); scenario screenshots are of synthetic fixtures only | they name the owner's players and lineup; the PR must not | `.gitignore`, `test_hygiene_no_roster_in_repo.py` |
| 2026-09-19 | Team codes go through `ids.nflverse_team` (Sleeper `LAR` → nflverse `LA`) | a Rams player rendered as BYE in the weekly report and would have projected 0 here; a team code is a join key | `gridiron.ids.TEAM_ALIASES` |
| 2026-09-19 | Screenshots use the pre-installed headless Chromium binary via subprocess when present, skipped with a message otherwise | no new dependency (no `playwright` package); the page is plain HTML and needs no browser to be useful | `dashboard_scenarios.screenshot` |
| 2026-09-20 | Kickoff locks are THREE-valued (LOCKED / OPEN / UNKNOWN); only OPEN makes a player movable | "not locked" previously meant both "kickoff is in the future" and "we have no idea", and only the first makes a lineup move legal. A two-valued lock cannot express the difference, so it resolved every unknown in the permissive direction | `lineup.Lock`, `Player.movable`, `test_lock_and_drop_regressions.py` |
| 2026-09-20 | A kickoff time is never invented; a game with no readable `gametime` yields UNKNOWN for both its teams | the old reader stamped a missing time as 13:00 ET, which is a guess that reads as fact, and it is wrong in the unsafe direction for every 9:30 am London game and every flexed night game | `lineup.kickoff_index`, `test_a_game_with_no_kickoff_time_is_unknown_not_one_oclock` |
| 2026-09-20 | "Not in the index" means BYE only when the week's schedule parsed completely AND the team plays elsewhere in the same frame | an empty index is indistinguishable from "all 32 teams are on bye"; an unreadable week used to authorise every swap on the board | `lineup.KickoffIndex.complete`, `test_a_week_that_could_not_be_read_is_never_a_league_wide_bye` |
| 2026-09-20 | A partially readable schedule freezes only the players it cannot time, by name, and still optimises the rest | abstaining from the whole week over one bad row throws away good information; pretending the row was fine throws away the user's roster | `lineup.plan_lineup`, `test_a_half_readable_schedule_freezes_by_name_and_invents_nothing` |
| 2026-09-20 | Freshness GATES actions, it does not merely label them: stale league / player-dump / injury / schedule inputs withhold lineup, waiver and matchup ACTIONS while keeping every comparison on the page, labelled as last-known | a DEGRADED banner over live-looking advice is a disclaimer the reader learns to scroll past. Deleting the comparison instead would destroy the one thing a stale cache is still good for | `gridiron.gating`, `test_stale_inputs_withhold_every_action_but_keep_the_comparison` |
| 2026-09-20 | Box-score sources (`weekly_stats`, `snap_counts`) deliberately gate nothing | they are stale by construction for most of every week — which the evidence boundary already states — so gating on them would withhold every action every Wednesday and train the reader to ignore the gate | `gating.GATED_SOURCES`, `test_box_score_staleness_does_not_withhold_anything` |
| 2026-09-20 | A source that is FRESH on disk but unreadable is still a blocker | a file's timestamp says when it was written, never whether its contents parse | `gating.build_gate(extra=...)` |
| 2026-09-20 | Bye / Out / IR / suspended players are PROTECTED from the automatic drop list and named with the reason, not ranked cheapest | their 0.0 is a statement about this week's availability, not about the player; ranking by projected points put the owner's injured RB1 first in line to be cut. Pricing them needs a rest-of-season model, and rule #5 does not allow one to ship unvalidated | `waivers.protected_players`, `Projection.is_withheld` |
| 2026-09-20 | Add eligibility is reported UNVERIFIED with its evidence; the board never says "add now" | the snapshot proves only that an id is unrostered at its as-of. The waiver clock, pending claims and the drop that created the vacancy live in Sleeper's transactions feed, which this repo does not pull | `waivers.eligibility`, `test_an_unrostered_player_is_never_promised_as_addable` |
| 2026-09-20 | The page is action-first, and uncalibrated P(win) ranks nothing | rule #7 denominates decisions in ΔP(win) — but this one has never been checked against a resolved matchup, so ranking start/sit calls by it would dress an unvalidated projection in a second unvalidated layer. Actions rank by deadline, then projected points; P(win) sits behind a disclosure | `dashboard.build_actions`, `test_the_page_leads_with_actions_...` |
| 2026-09-20 | "What changed" is diffed from the previous frozen archive, never recomputed, with a 1.5-point movement floor | rule #8: alerts fire on transitions, and a transition has to be big enough that a human would act differently. Reading an archive to build a newer page would also put yesterday's numbers inside today's evidence | `decisions.diff_archives`, `MOVE_POINTS` |
| 2026-09-20 | The dashboard is built in the cloud as a PRIVATE artifact, with the same two gates as the Sleeper sync; no Pages, no hosting, no auth service | the page names the owner's players, so a public run would be a roster export. The desktop task stays disabled and nothing about this path depends on it | `.github/workflows/dashboard-artifact.yml` |
| 2026-09-20 | Review screenshots are captured at MEASURED content height and at 500 px, with the true 390 px fit checked separately in an iframe | a fixed capture height silently truncates the page, and this headless build clamps its window to 500 px — asking for 390 crops the PNG and makes correct text look clipped. An unexecuted check is reported UNAVAILABLE, never as a pass | `dashboard_scenarios.horizontal_overflow`, `FitCheck` |
| 2026-09-20 | **Supersedes the "parsed completely ⇒ BYE" row above.** A bye is proven only when the frame shows the team playing BOTH BEFORE AND AFTER the week in question; row-parse success is no longer part of the test for absence | parsing every row that arrived is not evidence that every row arrived. A frame holding BUF/MIA in week 2 and only NYJ/NE in week 3 parses without a single error and made BUF a proven bye — a truncated pull is indistinguishable from a clean one by parse success alone. Bracketing is evidence the frame supplies about itself; no league-size constant or bye table is hardcoded | `lineup.KickoffIndex.proven_bye`, `test_a_week_that_simply_stops_early_is_not_a_league_of_byes` |
| 2026-09-20 | Two week rows giving one team two kickoffs discard BOTH times; a row naming one team is half a game and times nobody; a row listing one team on both sides is dropped | the reader silently kept whichever contradictory row it read last. A row that lost one of its two required team names is not a row whose kickoff field deserves more trust than its team field | `lineup.kickoff_index`, `KickoffIndex.conflicting`, `partial_rows` |
| 2026-09-20 | A frame that simply ends at the rendered week is a NOTE, not a gate: it freezes the absent teams' players individually and withholds nothing else | the blanket withhold it first produced fired on the fixture and would fire on every end-of-season render, which is exactly the "train the reader to ignore the gate" failure the box-score exemption exists to avoid. Per-player UNKNOWN is the proportionate response | `dashboard.build_dashboard`, `KickoffIndex.rows_intact` vs `.complete` |
| 2026-09-20 | **Supersedes the "box scores gate nothing" row above.** Box scores are not gated on AGE but ARE gated on COVERAGE: MISSING, a failed refresh, a hole inside the covered weeks, or more than one week behind the evidence boundary withholds every action | an unconditional exemption means a cache nobody refreshed since September keeps endorsing actions forever, and every projection built on it is a statement about a different season. Four days old on a Wednesday is the publication cadence; six weeks behind is a refresh that did not happen. One week of slack is the cadence (rule #8), not a fit | `gating.box_score_blockers`, `BOX_SCORE_LAG_WEEKS`, `test_box_scores_months_behind_the_evidence_boundary_do_withhold` |
| 2026-09-20 | A WITHHELD action renders neutral last-known wording and loses the imperative entirely; the badge alone is not the fix | the card said WITHHELD above and "Consider claiming X" / "Fill it with Y" / "start X over Y" below. A reader who skims the sentence has been told to act on a five-day-old roster. The badge is a label; the sentence is the instruction | `dashboard.Action.title`/`.body`, `test_a_withheld_card_states_the_last_known_picture_not_an_instruction` |
| 2026-09-20 | The swap card states the edge as a MULTIPLE of the combined uncertainty instead of asserting it is larger than it | z = Δ/√(SD²+SD²) IS that ratio, so z = 0.7 means the edge is 0.7× the uncertainty — smaller than it. The card claimed the opposite for the whole 0.5–1.0 band, in the most confident sentence on the page. The 0.5 noise floor is unchanged; only the claim is | `dashboard._edge_sentence` |
| 2026-09-20 | A decision archive is immutable: the filename carries an 8-hex content digest, and writing different content to an existing path raises rather than replacing | two pages written in the same second shared a path, so the second silently replaced the first and the first was simply gone. An archive is decision-time evidence, and evidence is not edited | `decisions.archive_path(record=...)`, `ArchiveCollision` |
| 2026-09-20 | "The previous archive" is selected by parsed write TIME, never by filename sort; a name that does not parse is skipped rather than ordered by guess | the week prefix dominates a name comparison, so `week05_<september>` sorted after `week04_<october>` and a month-old record was returned as the newest | `decisions.list_archives`, `archive_stamp` |
| 2026-09-20 | Grading separates hypothetical COMPARISONS from confirmed DECISIONS; a decision requires an observation passed in from outside, and withheld or eligibility-unverified pairs are excluded from the agreement rate | nothing in this project watches the owner. The roster still holding a player proves nobody removed him, not that anyone chose to keep him, and grading advice the page explicitly WITHHELD scores a recommendation that was never made | `decisions.grade_archive(observed=...)`, `GradedComparison.stance`, `test_advice_the_page_withheld_is_not_scored_as_advice` |
| 2026-09-20 | Frozen decision records are carried between ephemeral cloud runs through the repository's own Actions cache, with every restored file validated before it counts as history | a hosted runner starts empty, so every scheduled run was a first run: "since the last snapshot" always found nothing and the frozen page died with the container, which makes later grading impossible. A restored file is input, not history — filename-vs-contents, digest, season, future stamps and age are all checked. The cache is NOT durable (7-day idle eviction, 10 GB limit) and the docs say so rather than implying permanence | `gridiron.carryover`, `scripts/cloud/carryover.py`, `test_two_ephemeral_runs_carry_one_history_between_them` |
| 2026-09-20 | No `${{ ... }}` expansion may appear inside any workflow `run:` block; values reach scripts through `env:` | the runner substitutes expressions before the shell parses the script, so `${{ inputs.week }}` in a run block executes whatever was typed into the dispatch box. The invariant is enforced across every workflow with no per-line exceptions, including for GitHub's own step outcomes | `test_no_workflow_expands_an_expression_inside_a_shell_script`, `dashboard.py:week_from_env` |
| 2026-09-20 | **Supersedes BOTH bye rows above.** Absence is never evidence of a bye, by any route: a team with no row for the rendered week is UNKNOWN unless the schedule EXPLICITLY DECLARES a bye for it (`game_type: BYE`). Bracketing — the team plays before and after the gap — is removed, not replaced | the bracketing rule failed the test it was written for. Delete one real game from a full season frame and the two teams are still bracketed by the weeks either side, every row still parses, the frame still runs to week 18, and `proven_bye` hands the deletion back as a confirmed bye. A missing row and a week off are the same bytes; nothing computed from the rows that ARRIVED can separate them, so nothing is. nflverse declares no byes, so in practice the set is empty and absences are UNKNOWN — which is the honest answer, not a gap to fill with a third heuristic | `lineup.BYE_GAME_TYPES`, `KickoffIndex.declared_bye`, `test_deleting_one_real_game_from_a_full_season_frame_is_not_a_bye` |
| 2026-09-20 | The cloud carry moves the last-good INPUTS (manifest + frames) as well as the frozen records, with every `as_of` untouched and every restored entry marked as not refreshed by this run | `continue-on-error` on the refresh steps was an empty promise on a hosted runner: the cache is gitignored and dies with the container, so a run whose refresh failed had nothing to render FROM. It exited 2, wrote no page, failed the artifact upload — and the step summary said "Built." Carried inputs make that run produce a dated last-known page with every action withheld, which is the whole point of a degraded build. A successful refresh overwrites them and clears the mark for that source | `carryover.restore_inputs`/`publish_inputs`, `CARRIED_FORWARD`, `test_a_second_runner_with_every_refresh_failed_still_renders_last_known` |
| 2026-09-20 | Every field read out of a restored record is read TOTALLY (`_as_int`), one malformed file is rejected on its own and costs nothing beside it, and the filename's week must match the week inside the record | `int(blob.get("season") or 0)` raised `ValueError` on `season: "invalid"`, so a single hand-edited file aborted the whole restore — the malformed input got to stop the run, which is the outcome validation exists to prevent. The week check closes a gap the digest cannot see: the digest covers the contents, so a record filed under the wrong week is internally perfect and would be diffed against the wrong page | `carryover._as_int`, `inspect_archive`, `test_one_malformed_record_does_not_cost_the_sound_one_beside_it` |
| 2026-09-20 | The run summary's "Built." is printed only under the render step's actual outcome; a failed render says NOT BUILT | it was unconditional, and on a failed run it printed "Built." immediately above its own line "No dashboard was written." A summary that contradicts the job it summarises is worse than no summary | `.github/workflows/dashboard-artifact.yml`, `test_the_run_summary_never_claims_a_page_that_was_not_built` |
| 2026-09-20 | A carried manifest ENTRY is validated field by field before `ing.Entry` is constructed: `path` must be a plain filename that travelled with the carry, `rows` a non-negative count, `as_of` a parseable time, `weeks` a list of weeks, and an unknown field refuses the entry. A rejection costs that entry alone | `ing.Entry(**raw)` looks like validation and is not — a dataclass checks which KEYS it was handed and never what they hold. `path: ["bad"]` therefore constructed cleanly and raised `TypeError` at `Path(entry.path)`, killing the restore of every sound source beside it; `rows: "many"` and an unparseable `as_of` passed the carry entirely and raised inside `Manifest.freshness` half a render later, which is worse because carryover had already reported success. An ABSOLUTE path was the sharpest: basenamed for the copy, written into the manifest verbatim, and since `dir / "/abs"` discards the left operand in pathlib, `Manifest.file()` handed the renderer a file that never travelled with the carry and that nothing had validated. Basenaming a bad value is not a fix — it accepts the entry while changing what it means | `carryover._safe_basename`, `_validated_entry`, `test_an_absolute_carried_path_cannot_reach_outside_the_cache` |
| 2026-09-20 | A carried input file may never be written to a destination the local cache already holds. The collision is judged by DESTINATION FILENAME against every file present — case-folded on every platform, not only case-insensitive ones — and a conflict refuses the carried entry rather than overwriting | the first version compared the carried entry's NAME against the local manifest and then copied by FILENAME, which are different checks. A carried `weekly_stats` pointing at `shared.json` and a local `sleeper_state` pointing at the same file share no name, so the carry overwrote this run's current bytes with older ones — and left the LOCAL entry, with a current `as_of` and an empty `error`, vouching for bytes it had never seen. That is worse than stale data: an entry with no failure mark is exactly the one the gate does NOT withhold on, so the page would have recommended actions from it. Case folding is stricter than POSIX on purpose: the store is published by one machine and restored on another | `carryover._dest_key`, `restore_inputs`, `test_a_carried_file_never_displaces_a_local_one_under_another_name` |
| 2026-09-20 | Game Day shows PLATFORM ACTUALS only — Sleeper's `points`, `starters_points`, `players_points` and `custom_points` as sent, kickers and defenses included, zero and negative kept; a value not sent is UNKNOWN, never 0; the starters' sum is reconciled against the platform total and any difference is DISCLOSED, never edited away | the projection model cannot support K/DST and is unvalidated besides; on Sunday the only honest number is the one the platform scored. Summing in place of a missing total, or nudging a total to make it agree, would present an invented number with the platform's authority | `gameday._side_totals`, `test_a_value_the_platform_did_not_send_is_unknown_not_zero`, `test_an_unexplained_difference_is_disclosed_and_the_total_kept` |
| 2026-09-20 | A game's status is OBSERVED or UNKNOWN. It comes from Sleeper's per-game status feed (undocumented; five words seen and mapped, anything else passed through as UNKNOWN with the word shown), only while the feed is within a 30-minute gameday cadence; a stale feed keeps FINAL/CANCELED and demotes the rest. Elapsed time, a zero and an absent row never mean final, bye or not playing | a kickoff time is an expectation, not an event: delayed starts, overtime, suspensions and postponements all exist, and the one inference the page could make from the clock is the one that would be wrong exactly when it matters. The feed's real response was inspected before anything relied on it, and its limits (no clock, no quarter, undocumented) are stated on the page | `gameday.GameFeed.status_for`, `livesync.refresh_game_status`, `test_a_kickoff_that_has_passed_is_never_a_final_by_itself`, `test_observed_statuses_map_and_unknown_words_pass_through` |
| 2026-09-20 | Game status, injury designation, roster eligibility and the kickoff lock are four separate columns; where the feed and the schedule disagree, the disagreement is stated and the SAFER side decides legality (a feed "in_game" locks a player the schedule still calls open; a feed "pre_game" after the scheduled kickoff stays NOT STARTED but the lock follows Sleeper's real kickoff) | collapsing them produced every earlier lie in this repo's history: "not locked" meaning unknown, "0 points" meaning not playing. Two sources that disagree are information, not a tie to break in the permissive direction | `gameday.build_gameday.status_and_lock`, `test_feed_and_schedule_disagreements_are_stated_and_the_safer_side_locks` |
| 2026-09-20 | During games only moves Sleeper would accept are offered, re-judged from the board's archived actions BY PLAYER ID: ACTIONABLE pregame, decided before its own deadline, deadline still ahead, both players proven OPEN, not under way per the feed, still on the roster and still where the advice left them; pickups are never game-day moves; a WITHHELD pregame action stays withheld. Archived actions now carry `player_ids`, and archives carry `league_id` and `my_roster_id`, so the join is by id and never by name | a lineup that already differs is an observation, not proof the owner acted; a withheld comparison does not become advice because the games started; and re-optimising during play would be a new recommendation built on a projection the model cannot update honestly. Rule #3: the ids were the only thing missing from the archive, so they were added | `gameday._judge_action`, `dashboard.Action.player_ids`, `test_a_withheld_pregame_action_never_becomes_advice_during_games`, `test_an_action_whose_lineup_already_moved_is_an_observation_not_a_decision` |
| 2026-09-20 | The pregame record is JOINED (newest archive for the same season, week, league id and roster id; untagged records matched on season and week and said so) and never reconstructed; an action is decision-time evidence only if the record predates that action's deadline; a designation that moved after the record is reported with "the later status cannot show what was knowable before kickoff"; outcomes are UNPROVEN until both games are final and descriptive after | hindsight is the specific way a Sunday page scolds: the current Out designation, the bench score, the final margin were all unknown when the choice was made. Rule #7 grades the choice given what was known; nothing here grades at all | `gameday.find_pregame_record`, `test_the_record_must_match_season_week_league_and_roster`, `test_a_designation_that_moved_after_the_record_is_reported_without_blame`, `test_outcomes_are_descriptive_and_unproven_until_both_final` |
| 2026-09-20 | No live win probability and no projection arithmetic on Game Day: the board's UNCALIBRATED pregame P(win) is shown as pregame context only, and no projection is scaled by the clock or added to points already earned. What the page shows instead is remaining exposure in words: who on each side is yet to play, playing, suspended, unknown, final — and a lead is never called safe while anyone can still score | a live win number would be a third unvalidated layer (a clock-scaled projection on top of an unvalidated projection on top of an uncalibrated closed form) and rule #5 does not allow the first layer, let alone the third. "Ahead by 15 with their RB, K and DST to play" is the true statement and the useful one | `gameday.ScoreView.settled`, `test_a_lead_is_never_called_safe_while_they_have_players_left`, `test_no_live_win_probability_and_no_projection_arithmetic_on_the_page` |
| 2026-09-20 | Delivery stays the private artifact; the Game Day file refreshes itself from Sleeper's public read API on one tap (three GETs: NFL state, matchups, status feed), then polls only while visible and only while a game can still move, backs off on failure, keeps last good under STALE, discards responses dated before the one applied, and stops at a week rollover. The page's CSP allows one connect host, its own nonce script, and nothing else. Nothing is fetched on load | CORS was verified from a real `file://` page in a real browser, so the cheapest PC-independent path — the file the owner already downloads — works without hosting, auth, a tunnel, a desktop service or a dependency. A page that fetched on load would spend requests for every accidental open; one that polled in the background would spend them for nobody. Fixture drives show what the page does with each response; only the real-endpoint run shows the path works, and the docs say which is which | `gameday._JS`, `render_gameday_html` CSP, `test_refresh_applies_corrections_keeps_last_good_and_discards_older_responses`, `test_the_page_may_only_talk_to_sleeper` |
| 2026-09-20 (second pass) | An archived ACTIONABLE is historical evidence, never authority: Game Day re-runs every gate NOW — the league snapshot, player dump and schedule must all be current (a refresh renews only the first, and says so); the incoming player must be on the bench (not IR/taxi), not listed Out/IR/PUP/Sus/NA/COV/DNR (Questionable and Doubtful stay eligible, rule #11, and the card says the word), and eligible by `fantasy_positions` for the ACTUAL destination slot (FLEX takes RB/WR/TE; an empty slot must be named by the record or unique, and still empty); a game status the feed cannot CONFIRM is not legal advice. The score never depends on any of this | the first version accepted the board's verdict and re-checked only locks and roster membership, so it offered an RB into a WR slot, a player now Out, and a move resting on a 9-hour-old roster — each with a fresh-looking AVAILABLE badge. A withheld card that still shows the score is the honest degraded state; an available card built on stale or wrong inputs is the failure this repo exists against. Archived actions now carry `slot`; the schedule alone never confirms a game | `gameday._judge_action`, `dashboard.Action.slot`, `test_a_stale_input_withholds_…`, `test_the_incoming_player_must_be_eligible_…`, `test_a_player_the_platform_lists_out_is_never_offered`, `test_an_unconfirmed_game_status_is_never_legal_advice`, the `pregame` browser drive |
| 2026-09-20 (second pass) | A pregame record qualifies only with a valid, matching `league_id` AND `my_roster_id`, bytes that still match the digest in its own filename, well-formed identity fields and a generation time not in the future (5 min of clock drift allowed); untagged, altered, malformed or future archives are listed as context and never used, another league's or roster's are neither used nor listed. Each move is judged from the newest board written BEFORE its own deadline: a postgame render never displaces the pregame provenance, and a later decision-time board that dropped the move supersedes it | 'untagged, matched on season and week' accepted any league's archive for any roster as this owner's advice, and 'newest archive' let a Sunday-night render erase Saturday's decision-time evidence. Identity is exact or absent; time runs one way | `gameday.qualifying_records`, `select_evidence`, `test_an_untagged_record_is_context_only_…`, `test_a_record_whose_bytes_no_longer_match_its_digest_is_refused`, `test_a_newer_postgame_board_does_not_erase_the_pregame_evidence` |
| 2026-09-20 (second pass) | The page re-judges locks, source ages and legality at the present instant every 15 s, on every failed refresh and whenever the tab becomes visible — WITHOUT fetching. A build-time freshness flag is never frozen true; the same cadences the cloud build used age each source in the browser; a refresh renews roster, scores and statuses only and the status line says what it did not renew | `setInterval` used to update the age text only, so a card marked AVAILABLE at noon stayed AVAILABLE across the 13:00 kickoff on a phone left open or offline, and a failed refresh left the old action DOM in place. The clock is the one input the page always has | `gameday._JS` `tick`, `sourceState`, the `pregame` drive's `aged` / `refresh_sunday` / `cross_kickoff_idle` steps |
| 2026-09-20 (second pass) | Every request has its own timeout and the refresh chain always finishes (the button and backoff recover on any error); a matchup payload is validated field by field — roster ids present and unique, `matchup_id` present, `starters` a list of the last-good length for this roster, `points`/`custom_points` a number or null, `starters_points` a list of numbers and nulls, `players_points` an object — before it replaces anything, and a legitimately missing point stays null; the NFL state must carry a numeric season and week and a `season_type`, and a non-regular season type stops polling like a rollover; the feed must carry at least one usable row for this week; responses spanning over a minute are said not to describe one instant | `fetch` without a timeout let one hung request pin Refresh disabled forever, and `[{roster_id: 1}]` passed the old check and replaced a sound score and lineup with nothing. Validation that converts a missing value to 0 would be the other failure, so absence stays absence | `gameday._JS` `get`, `checkRows`, `checkState`, `checkFeed`, the `mixed` drive's `timeout` / `partial` / `duplicate` / `state_fail` / `feed_fail` steps |
| 2026-09-20 (second pass) | The inline script and the Python model are held to parity on one embedded payload under Node (pre-installed on the build machine, not a dependency; the test skips with reason without it), and the browser drives run in real time behind a held load event rather than under a virtual clock | the two implementations are hand-mirrored by design, and drift between them is silent: the Python tests would pass while the phone showed something else. Virtual time pauses while a fetch is pending, so a timeout could never be exercised under it; real time can | `tests/test_gameday_parity.py`, `gameday_scenarios._HARNESS` (`/__wait`, `/__done`) |
| 2026-09-21 | The repository AND the personalised pages (the owner's lineup, the advice and its rejected sides) are PUBLIC, by explicit owner decision; Astra flipped visibility and configured Pages (build type: workflow) at https://kejjeh.github.io/gridiron/. Credentials stay forbidden everywhere (rule #9); the workflows never write to Sleeper | the owner chose the personalised public dashboard over a shell. GitHub Pages from a private repository needs a paid plan, not Free — an earlier audit note had this backwards — so the public site and the public repository go together. History is not rewritten and no artifact is deleted | `docs/CLOUD_SYNC.md`, `HANDOFF.md` |
| 2026-09-21 | Both cloud workflows run on a public repository only under a second repository variable, `GRIDIRON_PUBLIC_PUBLICATION` exactly `true`; the private-repository path is unchanged. The Pages deploy is a separate job holding the only `pages: write` / `id-token: write`, SHA-pinned to `actions/upload-pages-artifact` v5.0.0 and `actions/deploy-pages` v5.0.1, on the schedule or a manual dispatch only, after a package step that succeeded | flipping visibility alone must stop everything (it did); publishing must be a deliberate second act. The permissions live nowhere near the code that installs and renders, and no `pull_request` trigger exists, so a fork cannot deploy or read the carry cache through these files | `.github/workflows/*.yml`, `tests/test_publication.py` |
| 2026-09-21 | The public site is exactly `index.html` (a redirect to Game Day), `gameday_latest.html` and `dashboard_latest.html`, copied byte for byte by `gridiron.publication`; a missing or empty page, a stray file, or a forbidden string (credential shapes, private tree paths, cache filenames) refuses the whole package with nothing written, so the last good site stays up. The dashboard workflow is hourly, best effort | the Pages artifact path is the packaged directory, never the render directory, so a JSON record cannot ride along by glob; nothing is restamped, so an older site is dated by its own pages. Hourly is what makes a public site useful on a game day; the page's own refresh covers scores, the build covers designations, and neither is a guarantee of live advice or injuries | `scripts/cloud/pages_site.py`, `test_the_package_is_exactly_the_allowlist_copied_verbatim`, `test_a_missing_page_refuses_the_whole_package_and_keeps_the_previous_one` |
| 2026-09-21 | A move is endorsed only when every player's game is positively reported pre-game by a current feed; a fresh feed carrying a status word the page does not know, and a canceled game on either side of the move, withhold it (score still shown), in Python and in the inline script alike | freshness proved the feed, not the game: the old gate excluded PLAYING/FINAL/SUSPENDED and let a current UNKNOWN through as legal. Starting a player in a canceled game is never advice, and a comparison the board priced assuming both games would be played is not re-modelled here | `test_a_fresh_feed_with_an_unknown_status_word_is_not_permission`, `test_a_canceled_game_on_either_side_withholds_the_move`, parity cases `unknown_word`, `canceled_incoming`, `canceled_outgoing` |
| 2026-09-21 | The parity test decodes Node's stdout as UTF-8 explicitly | on Windows `text=True` used the console codepage and every parity case failed on a typographic quote (Astra: 15 failures at `bb49650`, none under `PYTHONUTF8=1`). The browser harness pipes already decoded bytes as UTF-8 explicitly and were audited unchanged | `tests/test_gameday_parity.py` |
| 2026-09-21 | An acquisition is an upgrade ONLY when it improves THIS WEEK's best legal lineup; the raw point difference between two players at different positions ranks nothing and endorses nothing. Bench-only pickups go to a same-position WATCHLIST with the note that the lineup would not change and future value is unpriced; a position with no droppable player to compare against is a COVERAGE note, never a cross-position comparison | reproduced by Astra on the released code: `build_board` listed a backup QB (17) over a bench WR (3) as a +14 DEPTH upgrade and the public board told the owner to consider claiming two backup QBs with the lineup unchanged and availability UNVERIFIED. The module even claimed such moves pay off in bye weeks, with no future-week evidence. Raw points across positions do not measure roster utility, and no rest-of-season model exists to price them (rule #5) | `waivers.build_board`, `Watch`, `WaiverBoard.watchlist/.coverage`, `test_a_backup_qb_over_a_bench_wr_is_not_an_upgrade_of_any_kind` |
| 2026-09-21 | An acquisition card is CONDITIONAL, never an unqualified ACTIONABLE: its lineup arithmetic is current, its availability is not established from a cache and never will be. The card names the benefit, the displaced starter, the drop, the coverage kept, the evidence date, its limits and the exact Sleeper check | an ACTIONABLE badge beside an UNVERIFIED availability is a contradiction a phone reader resolves in favour of the badge | `dashboard.Action.conditional`, `build_actions`, `test_an_acquisition_is_conditional_never_an_unqualified_actionable` |
| 2026-09-21 | Every upgrade carries its feasible ALTERNATIVE drops with the gain each keeps; two cards that want the same cheapest drop are shown as an either/or with the next feasible drop for each named or ruled out | the old page listed two pickups that both cost the same bench player as if both could be made; a drop is spent once | `waivers.Upgrade.alternatives`, `test_two_pickups_that_cost_the_same_drop_are_an_either_or` |
| 2026-09-21 | Section 1 is the next decision: it labels the week its advice is for with dated evidence, the lock state of every starter, what can still be done, what must wait for next week's inputs, and a next-week preview read from schedule rows only (UNAVAILABLE where none exist; an absent team is UNKNOWN, never a bye). No next-week projection is made and this week's gains are never restated as next week's | on the Monday of a week the released page still ranked bench pickups by this week's points with every starter locked; the honest page says the week is locked, roster moves are the owner's with a waiver clock this page does not know, and week N+1 needs inputs that do not exist yet | `dashboard.NextDecision`, `next_decision`, `dashboard_scenarios.py slate_end`, `tests/test_next_decision.py` |
| 2026-09-21 (radar) | The Free Agent Radar is `build_board` written down for EVERY projected pool player, one verdict each (LINEUP / RESEARCH / COVERAGE / BELOW / LOCKED / UNKNOWN / UNRANKED) with the reason and the like-for-like comparator; unprojected players are counted as missing evidence and never listed with a number | a board that shows only what clears a bar hides the comparison the owner makes by hand, and a number for an unprojected or cross-position player would be invented | `waivers.Candidate`, `PositionCoverage`; `tests/test_radar.py` |
| 2026-09-21 (radar) | The archive carries a `radar` block by Sleeper id and `diff_radar` reports transitions only, separating REFRESHED (newer input timestamps) from REVISED (a number or verdict moved); a first run, a week rollover, another league or roster, a record without the block or another block version is "no comparison", never "no change" | a newer timestamp is not news, and an empty diff against nothing reads as "nothing changed" | `gridiron.radar`; `test_a_first_run_and_a_week_rollover_are_no_comparison_not_no_change`, `test_an_unchanged_rerender_raises_no_changed_badge` |
| 2026-09-21 (radar) | Both pages share one theme, one three-tab navigation and one build stamp; the published-build check asks the hosting origin only (`connect-src 'self'`), by conditional GET every five minutes while visible, backs off on failure, keeps one timer, offers a reload and never swaps content or filters under the reader; opened from disk it says it is off | a page that quietly re-renders itself is a page whose numbers cannot be cited; a page that polls a third party from the browser widens the connect policy | `gridiron.theme`; `scripts/weekly/radar_drive.py`; `test_the_radar_and_the_published_build_check_hold_up_in_a_real_browser` |
| 2026-09-21 (radar) | The dashboard workflow runs every 15 minutes at best; the wording on the workflow, the pages and the handoff says queued-not-run, and the player dump and nflverse cadences are unchanged, so the extra runs cost five small GETs each | the owner asked for constant comparison; GitHub's schedule is the only PC-free clock and it guarantees nothing | `.github/workflows/dashboard-artifact.yml`; `test_the_schedule_is_every_fifteen_minutes_and_the_dispatch_input_is_still_data` |
| 2026-09-23 (review) | A page carries the instant its gated evidence expires and each move's kickoff deadline, and re-judges both on the reader's clock | An age is a label, not a gate: a board left open overnight, or re-served by Pages after the cloud stopped, kept presenting moves as current. The instant is computed from the gate's own cadences, so nothing new is tuned | `freshness.expires_at`, `gating.valid_until`, `theme.VALIDITY_JS` |
| 2026-09-23 (review) | Radar block v2 carries a scoring digest; season, baseline, slots or scoring differing is "no comparison"; an older, unreadable, vanished or future-dated input is an evidence item, never a refresh; a roster-side change to a LINEUP move is a `lineup` change, not a projection change | Ids matching is not a like-for-like basis, and a newer-looking summary must not hide a regression | `gridiron.radar` |
| 2026-09-23 (review) | A carried input is judged by the age of the pull that fetched it, and every source refreshes at half the freshness limit IN EFFECT | The carried mark made every cloud run re-download every input (the 16 MB player dump ~96×/day at 15 min); a weekday threshold left game-day sources skipped while the gate withheld. Owner trade-off stated: the player dump is now ≤ 8×/day on game days, down from every run, against Sleeper's once-a-day ask | `scripts/ingest/pull_week.py` |
| 2026-09-23 (review) | The published-build check trusts only its own page (`data-page`) and compares build stamps as instants | A different page or a garbled stamp at the URL could announce a "newer build" | `theme.SNAPSHOT_JS` |
| 2026-09-24 | Sleeper's full player map is requested at most once per 24 h, counted by REQUEST (success or failure), logged before the GET in `player_map_requests.json` and carried between cloud runs; only inside 10:00–13:00 ET, only 10:00–10:20 ET with no history at all; one attempt, no retry; `--force` does not bypass it; an empty map is a failure | docs.sleeper.com "Fetch all players" (read 2026-09-24): "intended only to be used once per day at most to keep your player IDs updated". Supersedes the 2026-09-23 half-limit refresh for this source (3 h on game days). The window and the slot bound the damage if the carry stops saving or is lost: at most the runs in one window a day, not every run | `gridiron.sleeper.player_map_budget`, `scripts/ingest/pull_week.py`, `gridiron.carryover` |
| 2026-09-24 | Designation freshness is NOT widened to fit the budget: `sleeper_players` keeps 24 h / 6 h, so on game, waiver and designation days designation-dependent moves are withheld outside ~6 h after the day's request, as CHECK IN SLEEPER items naming the players and the status tag to read | No documented free source is fresher: nflverse injuries update once a day at 07:00 UTC (nflreadr data schedule); a filtered `players/nfl?position=` query is the same endpoint, not a loophole. Relabelling the pull time or widening the limit would present a day-old status as current | `gridiron.freshness`, `gridiron.gating.VERIFY`, `dashboard.action_desk` |
| 2026-09-24 | The pull step reuses the league snapshot the sync step published in the same run (no error or carried mark, ≤ 20 min, same league id) instead of fetching it again | Ten GETs became five, and one build no longer pairs two league states | `pull_week.reusable_league_snapshot`, `--reuse-league-snapshot 20` |
| 2026-09-24 | A started STARTER is never offered as a drop (cited rule); a started or unknown-kickoff BENCH player is an UNVERIFIED drop: a move needing it is CHECK IN SLEEPER, never executable; a verified drop is always preferred; fallback drops are counted per move, verified first | support.sleeper.com 3473234 (a started starter leaves only via a claim submitted before kickoff, stays locked, points count) and 4037431 (the commissioner lock blocks adds and drops). Nothing official covers a started bench player; the league's `bench_lock`, `disable_adds` and `daily_waivers` fields are not defined in Sleeper's API docs, so they are recorded, not relied on | `gridiron.waivers.drop_rule`, `build_board`, `dashboard.build_actions` |
| 2026-09-24 | The Next Decision section is the Action Desk: at most 3 supported cards (why now, benefit, cost, if not, check, valid until), shared-drop pickups as one card, CHECK IN SLEEPER, HOLD; the page's headline is the desk's verdict | One set of cards, prioritised for the first phone screen; nothing re-judged (statuses come from the gate, lock and drop rule); no model, ROS or probability change | `dashboard.action_desk`, `_desk_card` |
| 2026-09-24 | One visual system: flat surfaces, lime reserved for the primary state, a 13.5 px floor, a bottom tab bar on phones, provenance in a "Data & freshness" disclosure, diagnostics folded after the decision sections, Game Day opening on the score | The owner asked for a sleek product; the first phone screen carried metadata instead of a decision. Warnings stay in view by rule: the DEGRADED banner names the stale inputs, and the expiry strikes the headline and the "all current" chip | `gridiron.theme`, `dashboard.render_html`, `gameday.render_html` |
| 2026-09-24 | (Astra review of `4b63520`) The player-map request window is gone: once 24 h have passed since the last request on a TRUSTWORTHY ledger, the first run requests, at any hour. No trustworthy ledger (missing, unreadable, malformed, empty, future-dated) never requests: RECOVERY NEEDED on the page until a person's one-time bootstrap (`--player-map-bootstrap` / the workflow's `player_map_bootstrap` input), which is refused while the cached map shows a request < 24 h old. The cold slot and the automatic cold start are removed | Astra reproduced a delayed run at 13:02 ET being refused for a whole day only because of the window, and a lost cache requesting every day in the cold slot. The window was the bound on a carry that stops saving; the run-number check below replaces it. Supersedes the window and slot in the first 2026-09-24 row. The day's request drifts later by up to one run interval a day | `gridiron.sleeper.player_map_budget`, `test_a_delayed_run_catches_up_after_24h_at_any_hour`, `test_no_trustworthy_history_never_requests_at_any_hour` |
| 2026-09-24 | In the cloud (`--carried-history`) a request also needs the carried ledger to have been saved by the IMMEDIATELY previous workflow run (`checked_run == GITHUB_RUN_NUMBER - 1`); every run stamps it. No strict 24 h guarantee is claimed from the Actions cache | The cache is disposable. A carry that stops saving now stops requests (the gap never closes) instead of turning every run into one; a run whose save was lost delays the request by one run. What remains: a save lost right after a REQUEST, followed by one that works, costs one extra request. A run number is public on every run page and is not a credential | `test_a_cache_that_stops_saving_stops_requests`, `test_one_lost_save_after_a_request_costs_at_most_one_extra`, `docs/review/action-desk/budget-recovery-proof.txt` |
| 2026-09-24 | With a VERIFIED open active roster spot a pickup is an add with NO drop (same gain > 0 rule, lineup evaluation and lock rules); open spots = `roster_positions` minus IR/taxi entries, against players minus `reserve` and `taxi` — never a raw count. Unknown capacity (no `roster_positions`, no `reserve`/`taxi` field, inconsistent counts) keeps the drop and puts the capacity check on the card. Adds competing for the LAST open spot are one either/or | Astra: suggesting a player be given up for nothing is user-impacting, not harmless. The real league object carries `taxi: null` on every roster and all 12 rosters are full, so the real board is unchanged (cached before/after) | `gridiron.waivers.roster_capacity`, `build_board(capacity=)`, `tests/test_open_roster_spot.py` |
| 2026-09-24 | An unverified drop is checked by LOOKING in Sleeper (does it offer a drop for him), never by submitting one | The old copy said "try the drop in Sleeper", which is advice to make a real transaction to learn a rule | `gridiron.waivers.drop_rule`, `test_an_unverified_drop_is_checked_by_looking_not_by_dropping` |
| 2026-09-24 | First screen: each desk card shows a short exact move, one visible condition line, the key facts (gain, drop or "none — open roster spot", the one check, until) and the compare control; why-now, the six full answers and the full reasoning are under "Why & details". The DEGRADED banner names each stale input and its status in view and folds the per-input reasons. Game Day puts the as-of and Refresh inside the score card with a one-line status and folds how refresh works under "About refresh"; failures still read in the status line | Astra inspected the committed 375 px screenshots: the first card's benefit and cost sat below the fold under prose and metadata, and a paragraph about refresh dominated Game Day | `dashboard._desk_card`, `gameday.render_html`, `docs/review/action-desk/*-v2-*` |
| 2026-09-24 | The wording guard reads page COPY (`theme.page_copy`: stylesheets and style attributes removed, script strings kept); `env(safe-area-inset-bottom)` is restored with `viewport-fit=cover` so the phone tab bar clears the iPhone home indicator | A CSS identifier is not a claim that a lead is safe; the earlier removal traded a real layout defect for a test artefact. The guard still fails on the word in body text, bold text, a title attribute or a script-written string | `theme.VIEWPORT`, `test_the_wording_guard_ignores_css_but_still_catches_copy` |
| 2026-09-24 | (Astra review of `7d335ab`) A cloud run that cannot prove its carried ledger was saved by run N-1 on a first attempt (no stamp, another number, no run number, a re-run or unknown `GITHUB_RUN_ATTEMPT`) requests nothing and writes a gap mark (`gap_seen`, its own time) into the ledger; the next request waits 24 h after the LATER of the last request and the mark; the mark only moves later and a run-number stamp never clears it; the chain is checked before the 24 h wait; a re-run cannot bootstrap; readable evidence on a rejected ledger refuses a bootstrap like the map does | Astra reproduced two requests 30 min apart at `7d335ab` (a save lost after run 107's request, then run 108 stamped 106's history and saved), and a re-run of the same number did the same in 20 min. Every missing run started before the run that saw the gap, so its time bounds any lost request. Cost: each gap (lost save, cancelled queued run, early failure, re-run) delays the next request to 24 h after it; repeated loss pauses requests (the page says so) until saves work for a day. Boundary: increasing run numbers, non-overlapping runs, newest-or-nothing restore, agreeing clocks; a lost ledger is still a person's bootstrap | `gridiron.sleeper.player_map_budget`, `note_player_map_check`, `pull_week.py` (`GITHUB_RUN_ATTEMPT`), `test_a_lost_save_after_a_request_holds_the_next_one_for_a_day`, `test_a_re_run_of_the_same_run_number_never_requests`, `test_repeated_and_intermittent_lost_saves_never_request_twice_in_a_day`, `docs/review/action-desk/budget-recovery-proof.txt` |
| 2026-09-24 | (Astra review of `471980c`) A REJECTED player-map ledger keeps every stamp that still parses (request times of any outcome, a gap mark) as bootstrap-only evidence; `carryover` lays a rejected carried ledger down verbatim, reported refused, instead of dropping it | One malformed field (`checked_run: "broken"`) erased a request an hour old and a 30-minute-old gap mark, and a bootstrap with an old or no map requested. Automatic requests are unchanged (the ledger stays RECOVERY NEEDED); evidence never authorises and the file is never sanitised. Limit: a ledger that is not JSON shows nothing | `gridiron.sleeper.read_player_map_history` (`evidence`), `player_map_budget`, `carryover._restore_ledger`, `test_a_malformed_field_does_not_erase_recent_evidence`, `test_a_rejected_carried_ledger_still_refuses_a_cloud_bootstrap` |
| 2026-09-25 | A carried input this run did not refresh reads "STALE (not refreshed)" / "… CARRIED FORWARD … (restored <time>)", never "REFRESH FAILED"; `SourceFreshness.carried` changes only the words — `refresh_failed`, the status, the as-of and every gate are unchanged, and a real failure still reads REFRESH FAILED | Production runs #23/#24 (RECOVERY NEEDED) made no map request, yet the banner said the designations' refresh FAILED, which points the owner at a broken Sleeper call instead of the bootstrap they need | `gridiron.ingest.Manifest.freshness`, `tests/test_carried_is_not_a_failure.py` |
| 2026-09-25 | No build interval is promised. Scheduled runs of both workflows arrived 2.0-7.8 h apart (median ~5 h) whether main's cron was hourly or every 15 min; none was cancelled, queued, re-run or failed, so the missing slots are schedule events GitHub never delivered (cause inside GitHub unknown). No more cron slots; a `workflow_run` build after each sync is PROPOSED, not adopted | The run history, not elapsed time, rules out repository-side causes; GitHub documents that scheduled events are delayed and "may be dropped" under load | `docs/review/schedule/schedule-gaps-2026-09-25.txt` |
